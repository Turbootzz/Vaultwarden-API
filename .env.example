# Server Configuration
API_PORT=8080
ENVIRONMENT=development

# Security - REQUIRED
# Generate a strong random API key (e.g., using: openssl rand -base64 32)
API_KEY=your-super-secret-api-key-here

# Vaultwarden Configuration - REQUIRED
# URL of your Vaultwarden instance (no trailing slash)
VAULTWARDEN_URL=https://vault.yourdomain.com

# Vaultwarden API access token
# Get this from: Vaultwarden > Settings > Security > API Key
# Or create via CLI: vaultwarden hash --preset <preset>
VAULTWARDEN_ACCESS_TOKEN=your-vaultwarden-access-token

# Performance & Caching
# How long to cache secrets in memory (reduces API calls)
# Set to 0 to disable caching
CACHE_TTL=5m

# Timeouts
READ_TIMEOUT=10s
WRITE_TIMEOUT=10s

